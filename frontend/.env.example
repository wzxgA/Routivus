# 前端开发环境变量（Vite 仅暴露 VITE_ 前缀）
# 后端服务地址，默认 http://127.0.0.1:18765，仅 dev server 代理使用
ROUTIVUS_SERVER_URL=http://127.0.0.1:18765

# 当后端配置了 ROUTIVUS_SERVER_TOKEN 时，前端需要同一个 token
# 该值会随 REST 请求头与 WebSocket 查询参数一起发送
VITE_ROUTIVUS_TOKEN=
